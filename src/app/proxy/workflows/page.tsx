import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Memory Flow - 工作流管理',
}

const WorkFlowsPage = () => {
    return <div>
        WorkFlowsPage
    </div> 
}

export default WorkFlowsPage